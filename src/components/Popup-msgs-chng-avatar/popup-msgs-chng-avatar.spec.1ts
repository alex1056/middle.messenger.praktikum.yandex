import { expect } from 'chai';
import { PopupMsgsChngAvatar } from './index';

describe('Тест render ф. в PopupMsgsChngAvatar', () => {
  it('должен быть возвращен html', () => {
    const popupMsgsChngAvatar = new PopupMsgsChngAvatar({});
    // console.log(profileFormCtrls);
    // console.log(popupMsgsChngAvatar.getContent());
    expect(
      popupMsgsChngAvatar.render().indexOf('<div class="popup popup_visible popup_msg" id="user-menu-popup">'),
    ).is.equal(0);
  });
});
